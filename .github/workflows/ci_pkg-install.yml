name: Check Installation

on:
  push:
    branches: [master, "release/*"]
  pull_request:
    branches: [master, "release/*"]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}-${{ github.head_ref }}
  cancel-in-progress: ${{ ! (github.ref == 'refs/heads/master' || startsWith(github.ref, 'refs/heads/release/')) }}

jobs:
  install:
    uses: ./.github/workflows/_check-install.yml
    # TODO: Update to reuse the workflow in a specific hash/branch/tag.
    # uses: PyTorchLightning/pytorch-lightning/.github/workflows/_check-install.yml@master
