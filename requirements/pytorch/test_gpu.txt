# torch-tensorrt hard-pins to a single torch minor and lags new torch releases, so listing it here
# would make the whole GPU install unresolvable. It is installed best-effort in the CI workflow
# instead; when no matching build exists, `RunIf(tensorrt=True)` tests skip.
